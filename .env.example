# Next.js Frontend Environment Variables
# Kopiere diese Datei zu .env.local und passe die Werte an

# Backend API URL
# Development:
NEXT_PUBLIC_API_URL=http://localhost:8120/rest/api/v1
# Production:
# NEXT_PUBLIC_API_URL=https://backend.simpliant-ds.eu/rest/api/v1

# Voxtral transcription backend (stored in user settings)
# Development:
NEXT_PUBLIC_VOXTRAL_BACKEND_URL=https://transcribe.simpliant-ds.eu

# NextAuth Configuration
# Development:
NEXTAUTH_URL=http://localhost:3005
# Production:
# NEXTAUTH_URL=https://app.simpliant-ds.eu

# NextAuth Secret (WICHTIG: Generiere einen neuen für Production!)
# Generieren: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here

# NextAuth Backend URL
# Development (muss mit NEXT_PUBLIC_API_URL übereinstimmen):
NEXTAUTH_BACKEND_URL=http://localhost:8120/rest/api/v1
# Optional: Interner Backend-URL (z. B. in Docker-Containern)
NEXTAUTH_INTERNAL_BACKEND_URL=http://localhost:8120/rest/api/v1
# Production:
# NEXTAUTH_BACKEND_URL=https://backend.simpliant-ds.eu/rest/api/v1
# NEXTAUTH_INTERNAL_BACKEND_URL=http://backend:8000/rest/api/v1
